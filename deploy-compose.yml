name: ecomart-app

services:
  ecomart-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecomart-service
    ports:
      - "8081:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      JAVA_OPTS: "-Xms256m -Xmx512m"
    restart: unless-stopped
