name: ecomart-app

services:
  joi-energy-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: joi-energy-api
    ports:
      - "8081:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      JAVA_OPTS: "-Xms256m -Xmx512m"
    restart: unless-stopped
