# Changes Summary

## 2026-06-10 Documentation Sync

Updated markdown files to match current repository behavior:

- aligned API docs to the 4 implemented endpoints
- aligned error contract docs to `ErrorResponse` fields
- removed outdated references to non-existent response fields (for example `errorId` in JSON)
- corrected repository status docs to reflect current test state (`test` pass, `functionalTest` currently failing)
